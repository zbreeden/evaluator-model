// Model button behavior removed: the header now contains a direct link to the model page.
// This file intentionally left minimal; other UI behaviors are implemented inline in `index.html`.

// No-op export (keeps file valid if other scripts import it)
Object.defineProperty(window, '__evaluatorModelScript', { value: true });
